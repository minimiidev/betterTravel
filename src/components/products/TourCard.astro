---
// import Icon from "astro-iconify";

// interface Props {
//   imgSrc: string;
//   name: string;
//   slug: string;
//   country: string;
//   price: number;
//   currency: string;
//   initialDate?: string;
//   finalDate?: string;
//   grade?: string;
//   description: string;
// }

// const {
//   name,
//   currency,
//   finalDate,
//   initialDate,
//   slug,
//   grade,
//   imgSrc,
//   country,
//   price,
//   description,
// } = Astro.props;
---

<!-- 
<article
  class="relative flex h-[300px] w-[350px] flex-col items-center justify-center gap-2 rounded-md bg-white shadow-md transition-all duration-300 ease-in-out hover:bg-slate-200"
>
  <div class="w-full h-[150px] overflow-hidden rounded-t-md">
    <img
      src={imgSrc}
      alt={name}
      class="h-full w-full object-cover transition-all duration-[1s] ease-in-out hover:scale-105"
    />
  </div>
  <div class="flex-1 w-full px-4">
    <div class="mb-2">
      <h2 class="h2">{name}</h2>
      <p>{country}</p>
    </div>
    <div class="flex items-center justify-between py-2 border-t border-b">
      <p>{initialDate}</p>
      <p class="font-bold"><span>{currency}</span>$ <span>{price}</span></p>
    </div>
  </div>
  <a
    href={`/tours/${slug}`}
    class="absolute -bottom-5 z-10 min-w-[120px] rounded-md border bg-sky-500 py-2 text-center font-bold text-white"
    >Mas Detalles</a
  >
</article> -->
