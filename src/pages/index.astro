---
import { Hero, Content, Footer } from "@/components/sections";
import Layout from "../layouts/Layout.astro";

const url = Astro.url.pathname;
const titleUrl = url.split("/").at(-1);
console.log(titleUrl);
---

<Layout
  title="Better Travel | Agencia de viajes"
  description="Reservas de hoteles, resorts o vuelos"
>
  <Hero />
  <Content />
  <Footer />
</Layout>
